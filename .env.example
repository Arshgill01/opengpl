# Real calls stay disabled unless both values below are set.
CALLE_API_KEY=
OPENGPL_LIVE_MODE=disabled

# Comma-separated E.164 numbers that the prototype is allowed to call.
OPENGPL_ALLOWED_NUMBERS=

# Optional public HTTPS endpoint for CALL-E terminal events.
OPENGPL_WEBHOOK_URL=
